<%= @genre.name %>
<% @genre.artists.each do | artist | %>
  <%= artist.name %>
    <a href="/artists/<%= artist.slug %>">artist</a>
<% end %>

<% @genre.songs.each do | song | %>
  <%= song.name %>
    <a href="/songs/<%= song.slug %>">song</a>
<% end %>
