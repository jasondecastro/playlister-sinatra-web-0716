<%= @song.artist.name %>
<% @song.genres.each do | genre | %>
  <%= genre.name %>
<% end %>
<a href="/artists/<%= @song.artist.slug %>">artist</a>
<% @song.genres.each do | genre | %>

  <a href="/genres/<%= genre.slug %>"><%=genre.name%></a>
  <%= genre.name %>
<% end %>

<%= @song.name %>


<%= @message %>
